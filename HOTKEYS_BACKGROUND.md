# 🔥 Система горячих клавиш с поддержкой фонового режима

## 🎯 Обзор

Система горячих клавиш теперь поддерживает работу в фоновом режиме с максимально возможной совместимостью:

### ✅ Веб-браузеры
- **Улучшенная обработка событий** - множественные слушатели на разных уровнях
- **Работа при потере фокуса** - горячие клавиши активны даже когда окно не в фокусе
- **Умная фильтрация** - игнорирует поля ввода только когда окно в фокусе
- **Подробное логирование** - отслеживание состояния окна и страницы

### ✅ Electron приложения  
- **Глобальные горячие клавиши** - работают даже когда приложение свернуто
- **Автоматическая регистрация** - система сама определяет Electron и регистрирует shortcuts
- **Полная совместимость** - поддерживает все модификаторы и функциональные клавиши

## 🚀 Как это работает

### Веб-браузер режим:
```typescript
// Множественные слушатели для максимального покрытия
document.addEventListener('keydown', handler, true); // capture phase
window.addEventListener('keydown', handler, true);
document.addEventListener('keydown', handler, { passive: false, capture: true });

// Умная логика обработки
if (document.hasFocus() && isInputFocused) {
  return; // Игнорируем только если окно в фокусе И фокус на поле ввода
}
```

### Electron режим:
```typescript
// Автоматическая регистрация глобальных shortcuts
window.electronAPI.registerGlobalShortcut('CommandOrControl+F1', callback);
```

## 📋 Функциональность

### 🎮 Поддерживаемые клавиши:
- **F1-F12** - функциональные клавиши
- **Модификаторы** - Ctrl, Alt, Shift
- **Специальные** - Space, Tab, Insert, Delete, Home, End, etc.

### 🔧 Автоматическое определение среды:
```typescript
const isElectron = () => {
  return typeof window !== 'undefined' && 
         window.process && 
         window.process.type === 'renderer';
};
```

### 📊 Подробное логирование:
```
🔥 Hotkey triggered: f1 (Переключить микрофон) - Window: background, Page: visible
🔥 Electron Global Hotkey triggered: f1 (Переключить микрофон)
🔥 Window focused - hotkeys active
🔥 Window blurred - hotkeys still active
```

## ⚠️ Ограничения веб-браузеров

### Что НЕ работает в браузерах:
- **Истинно глобальные горячие клавиши** - браузеры не позволяют по соображениям безопасности
- **Работа при полном сворачивании** - зависит от браузера и ОС
- **Системные комбинации** - Alt+Tab, Ctrl+Alt+Del и т.д.

### Что РАБОТАЕТ в браузерах:
- **Горячие клавиши при потере фокуса окна** ✅
- **Работа в фоновых вкладках** ✅  
- **Обход полей ввода в фоне** ✅
- **Множественные слушатели** ✅

## 🎯 Рекомендации по использованию

### Для максимальной совместимости:
1. **Используйте F-клавиши** (F1-F12) - лучшая совместимость
2. **Избегайте системных комбинаций** - Ctrl+C, Alt+Tab и т.д.
3. **Тестируйте в разных браузерах** - поведение может отличаться

### Рекомендуемые клавиши для микрофона:
- **F1** - основная рекомендация (по умолчанию)
- **F2** - для звука/deafen (по умолчанию)  
- **F3-F12** - дополнительные функции
- **Space + модификатор** - альтернатива

## 🔧 Настройка для Electron

Если вы используете Electron, добавьте в main процесс:

```javascript
const { globalShortcut } = require('electron');

// Регистрация в main процессе
globalShortcut.register('CommandOrControl+F1', () => {
  // Отправить событие в renderer процесс
  mainWindow.webContents.send('global-shortcut', 'f1');
});
```

## 📈 Результат

### ✅ Достигнутые улучшения:
- **Работа в фоне** - горячие клавиши активны при потере фокуса
- **Electron поддержка** - истинно глобальные shortcuts
- **Умная фильтрация** - не мешает вводу текста
- **Подробная диагностика** - полное логирование состояний
- **Максимальная совместимость** - работает везде где возможно

### 🎯 Использование:
```typescript
// Система автоматически определяет среду и использует лучший доступный метод
const hotkeys = [
  {
    key: 'f1',
    callback: toggleMicrophone,
    description: 'Переключить микрофон'
  }
];

useHotkeys(hotkeys); // Работает в фоне!
```

**Горячие клавиши теперь работают максимально эффективно в любой среде!** 🔥
