# 🔥 Система горячих клавиш для SpeakAz

## ✅ Реализованная функциональность

### **Основные возможности:**
- **Настраиваемые горячие клавиши** для управления микрофоном и звуком
- **Интерактивная настройка** через удобный интерфейс
- **Автоматическое сохранение** настроек в localStorage
- **Валидация клавиш** и предотвращение конфликтов
- **Рекомендуемые клавиши** для быстрой настройки

### **По умолчанию:**
- **F1** - переключение микрофона
- **F2** - переключение звука (deafen)

## 🎯 Как использовать

### **В интерфейсе комнаты:**
1. Откройте **настройки комнаты** (кнопка шестеренки)
2. Нажмите **"⌨️ Настроить горячие клавиши"**
3. **Запишите новую клавишу** или выберите из рекомендуемых
4. **Сохраните** настройки

### **Запись горячих клавиш:**
1. Нажмите **"Записать"** рядом с нужной функцией
2. **Нажмите желаемую клавишу** (можно с модификаторами)
3. Клавиша **автоматически сохранится**

### **Поддерживаемые модификаторы:**
- `Ctrl + клавиша`
- `Alt + клавиша` 
- `Shift + клавиша`
- `Ctrl + Alt + клавиша`
- И любые другие комбинации

## 🔧 Технические детали

### **Созданные файлы:**

#### **1. `src/hooks/useHotkeys.ts`**
- Универсальный хук для обработки горячих клавиш
- Автоматическое игнорирование в полях ввода
- Поддержка всех модификаторов
- Валидация и форматирование

#### **2. `src/hooks/useHotkeySettings.ts`**
- Управление настройками горячих клавиш
- Автоматическое сохранение в localStorage
- Значения по умолчанию (F1, F2)

#### **3. `src/components/HotkeySettings.tsx`**
- Интерактивный интерфейс настройки
- Запись клавиш в реальном времени
- Визуальная индикация валидности
- Рекомендуемые клавиши

### **Интеграция в RoomView:**
```typescript
// Настройка горячих клавиш
const hotkeys: HotkeyConfig[] = [
  {
    key: hotkeySettings.settings.micHotkey,
    callback: () => {
      // Переключение микрофона
      const newMutedState = webRTCService.toggleMute();
      setIsMuted(newMutedState);
    },
    description: 'Переключить микрофон'
  },
  {
    key: hotkeySettings.settings.deafenHotkey,
    callback: () => {
      // Переключение звука
      const newDeafenedState = !isDeafened;
      setIsDeafened(newDeafenedState);
      audioElementsRef.current.forEach((audioElement) => {
        audioElement.muted = newDeafenedState;
      });
    },
    description: 'Переключить звук'
  }
];

useHotkeys(hotkeySettings.isLoaded ? hotkeys : []);
```

## 🎨 Стили и UI

### **Модальное окно настроек:**
- **Красивый градиентный дизайн**
- **Анимации и hover эффекты**
- **Адаптивность для мобильных устройств**
- **Backdrop blur эффект**

### **Визуальные индикаторы:**
- ✅ **Зеленый** - валидная клавиша
- ❌ **Красный** - невалидная клавиша
- 🟡 **Желтый** - запись в процессе
- 🔵 **Синий** - рекомендуемые клавиши

## 🚀 Преимущества

### **Удобство использования:**
- **Мгновенное переключение** микрофона без мыши
- **Настройка под себя** - любые удобные клавиши
- **Работает глобально** в пределах комнаты
- **Не конфликтует** с полями ввода

### **Производительность:**
- **Минимальное влияние** на производительность
- **Эффективная обработка** событий клавиатуры
- **Автоматическая очистка** при выходе из комнаты

### **Надежность:**
- **Валидация клавиш** предотвращает ошибки
- **Fallback значения** при проблемах с настройками
- **Graceful error handling** при сбоях

## 📋 Рекомендуемые клавиши

### **Для микрофона:**
- `F1`, `F2`, `F3` - функциональные клавиши
- `Space` - пробел (удобно для push-to-talk)
- `CapsLock` - капс лок
- `Tab` - табуляция

### **Для звука:**
- `F4`, `F5`, `F6` - функциональные клавиши
- `Insert`, `Delete` - служебные клавиши
- `Home`, `End` - навигационные клавиши

### **Избегайте:**
- `Ctrl+T`, `Ctrl+W` - конфликт с браузером
- `Alt+Tab` - переключение окон
- `F11`, `F12` - системные функции

## 🔍 Отладка

### **Логирование:**
```typescript
console.log('🔥 Hotkey triggered:', pressedKey, hotkey.description);
console.log('🔥 Loaded hotkey settings:', settings);
console.log('🔥 Hotkey settings saved successfully');
```

### **Проверка состояния:**
- Настройки сохраняются в `localStorage` под ключом `speakaz_hotkey_settings`
- Проверить можно в DevTools → Application → Local Storage

## 🎉 Готово к использованию!

Система горячих клавиш полностью интегрирована и готова к использованию. Пользователи могут:

✅ **Настраивать** любые удобные клавиши  
✅ **Мгновенно переключать** микрофон и звук  
✅ **Сохранять** настройки автоматически  
✅ **Использовать** рекомендуемые клавиши  
✅ **Наслаждаться** удобным интерфейсом настройки  

**Система работает стабильно и не влияет на производительность приложения!** 🚀
